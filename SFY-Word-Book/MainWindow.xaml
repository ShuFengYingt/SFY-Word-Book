<Window x:Class="SFY_Word_Book.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:SFY_Word_Book"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="400">
    <Window.Resources>
        <sys:String x:Key="myString">Hello WPF Resources!</sys:String>
    </Window.Resources>
    <StackPanel Background="LightBlue">
        <TextBox Text="{StaticResource ResourceKey=myString}" Margin="5"/>
        <TextBox Margin="5" x:Name="textbox_1"/>
        <Button Content="Show" Click="Button_Click" Margin="5"/>
        
        <!--在local下找到MyButton类，此类派生于Button，设置属性UserWindow扩展标签为x:Type扩展 本地MyWindow,当按钮被点击时激发OnClick,调用弹出MyWindow-->
        <local:MyButton Content="MyWindow" UserWindowType="{x:Type local:MyWindow}" Margin="5"/>
    </StackPanel>


</Window>

